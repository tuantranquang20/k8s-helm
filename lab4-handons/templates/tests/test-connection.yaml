apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab4-handons.fullname" . }}-test-connection"
  labels:
    {{- include "lab4-handons.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab4-handons.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
