apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab4-handons.fullname" . }}
  labels:
    {{- include "lab4-handons.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab4-handons.selectorLabels" . | nindent 4 }}
