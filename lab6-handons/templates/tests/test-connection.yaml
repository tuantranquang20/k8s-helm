apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab6-handons.fullname" . }}-test-connection"
  labels:
    {{- include "lab6-handons.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab6-handons.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
