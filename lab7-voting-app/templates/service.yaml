apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab7-voting-app.fullname" . }}
  labels:
    {{- include "lab7-voting-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab7-voting-app.selectorLabels" . | nindent 4 }}
