apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab7-voting-app.fullname" . }}-test-connection"
  labels:
    {{- include "lab7-voting-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab7-voting-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
