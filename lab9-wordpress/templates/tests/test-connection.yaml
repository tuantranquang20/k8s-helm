apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab9-wordpress.fullname" . }}-test-connection"
  labels:
    {{- include "lab9-wordpress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab9-wordpress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
