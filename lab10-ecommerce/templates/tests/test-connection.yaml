apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lab10-ecommerce.fullname" . }}-test-connection"
  labels:
    {{- include "lab10-ecommerce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lab10-ecommerce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
