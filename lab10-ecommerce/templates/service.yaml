apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab10-ecommerce.fullname" . }}
  labels:
    {{- include "lab10-ecommerce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab10-ecommerce.selectorLabels" . | nindent 4 }}
