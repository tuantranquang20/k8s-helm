apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: custom-app
  labels:
    app: nginx-app
    component: config
data:
  app-name: "my-app"
  environment: "development"
  log-level: "info"
