apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab8-monitoring.fullname" . }}
  labels:
    {{- include "lab8-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab8-monitoring.selectorLabels" . | nindent 4 }}
